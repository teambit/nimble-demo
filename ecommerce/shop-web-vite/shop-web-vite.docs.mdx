---
description: 'A shop-web-vite app.'
labels: ['app', 'react']
---

A React application for shop-web-vite. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use shop-web-vite 
```

To run the app locally, run the following:

```
bit run shop-web-vite 
```
