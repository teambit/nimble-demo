---
description: 'A shop-web app.'
labels: ['app', 'react']
---

A React application for shop-web. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use shop-web 
```

To run the app locally, run the following:

```
bit run shop-web 
```
